name: Database script - Production

on:
  push:
    branches:
      - main
      - develop
      - stage

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: lynxnetworkbrasil/liquibase/.github/workflow/database-production/action.yml@main
