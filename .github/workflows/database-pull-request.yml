name: Database script - Pull Request

on:
  pull_request:
    paths:
      - 'database-script/**'

jobs:
  build:
    uses: lynxnetworkbrasil/liquibase/.github/workflows/database-pull-request.yml@main
